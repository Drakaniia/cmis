backup data is still .json or .db,  i want to to also have manual option, backup or export ad very detailed xlsx,  that is importable to the same app when using this app

so when i fillup template,, and try to import it, will it now work?


"you are  on latest versoin" taost when i click check for update in the help navbar and in settings

in the first load, its blank or stuck,  what does it called when logo shows in first click of the desktop app?  so opening app flow feels so smooth.
78bnd

remove the rotating when dragging cards in the queue,  how to make it fully smooth?  so when succes drag in another tab, it smooth animtate, it wont magnet blink,  similarly when cannot place it, it will auto pull smooth back to its place in animation

make all the text that cannot be highlightable.